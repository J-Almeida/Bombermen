package logic;

/**
 * The Enum EventType (RTTI).
 */
public enum EventType
{
    // Don't change Order because it is needed for EventComparator
    /** RequestMovement event. */
    RequestMovement,

    /** SendEagle event. */
    SendEagle,

    /** Movement event. */
    Movement,
}
